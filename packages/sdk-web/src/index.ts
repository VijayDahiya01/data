/**
 * @oolix/ad-sdk-web -- spec v5 §68.
 *
 * Rendering, timeout/fallback, visibility hooks and click handling only.
 * No targeting rules, no customer identifier, no Oolix endpoint.
 *
 * Semantic Versioning: breaking API changes increment major, and a Partner
 * integration pins a compatible range (§68).
 */
export * from './types.js';
export * from './client.js';
