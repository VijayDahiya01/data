# Findings gitleaks reports that are not secrets, pinned by fingerprint
# (commit:file:rule:line) so that each entry silences exactly one match and
# nothing else. A new occurrence of either pattern is still reported.
#
# CI scans only the pushed commits on a push, but a manual run scans the whole
# history -- which is where both of these live.

# An unsigned alg:none token, {"alg":"none"}.{"sub":"admin"}. -- the adversarial
# probe sends it deliberately to prove the API rejects a forged bearer token.
534e1120602a5e7069005358bb67ec1e51685338:scripts/pen-probe.mjs:jwt:128

# The `<Spaces secret>` placeholder in the example environment file.
42bc328c69e339937d9a121e98a87b3f82385298:docs/DEPLOY-DIGITALOCEAN.md:generic-api-key:212

# Two entries of the common-password list -- 'linkedin2012' and 'linkedinpass'
# -- each followed by a 16-character password, which is the shape of the
# linkedin-client-secret rule. The list is public breach data that sign-up
# refuses (oolix/packages/auth-rbac/src/common-passwords.ts), not a credential.
665a28befaa33693b6929223d9c9de8938949959:oolix/packages/auth-rbac/src/common-passwords.ts:linkedin-client-secret:631
665a28befaa33693b6929223d9c9de8938949959:oolix/packages/auth-rbac/src/common-passwords.ts:linkedin-client-secret:633
